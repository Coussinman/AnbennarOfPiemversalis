country_decisions = {
    WL_parliament_petition_decision = {
		major = yes
		potential = {
			has_country_flag = WL_unlocked_parliament_petition
		}
		allow = {
            hidden_trigger = { NOT = { has_country_flag = WL_petition_timeout } }
		}
		effect = {
            country_event = { id = wl.2 }
        }
    }
}